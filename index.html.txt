<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jungle Tiers | Elite Ranking</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-aqua: #e0f7fa;
            --deep-aqua: #00bcd4;
            --accent-red: #FF0000;
            --glass-bg: rgba(255, 255, 255, 0.2);
            --text-white: #ffffff;
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #4dd0e1, #00acc1);
            background-attachment: fixed;
            color: var(--text-white);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* --- Navbar --- */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            height: 80px;
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 5%;
            z-index: 1000;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .logo span { color: var(--accent-red); }

        .nav-links {
            display: flex;
            list-style: none;
        }

        .nav-links li { margin: 0 20px; }

        .nav-links a {
            text-decoration: none;
            color: var(--text-white);
            font-weight: 600;
            transition: var(--transition);
        }

        .nav-links a:hover { color: var(--accent-red); }

        .hamburger {
            display: none;
            cursor: pointer;
            font-size: 1.5rem;
        }

        /* --- Hero Section --- */
        #hero {
            height: 90vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 20px;
            margin-top: 80px;
        }

        #hero h1 {
            font-size: clamp(2.5rem, 8vw, 5rem);
            margin-bottom: 20px;
            text-shadow: 2px 4px 10px rgba(0,0,0,0.2);
        }

        .cta-group {
            display: flex;
            gap: 20px;
        }

        .btn {
            padding: 12px 30px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            border: none;
            cursor: pointer;
        }

        .btn-primary {
            background: var(--accent-red);
            color: white;
        }

        .btn-primary:hover {
            transform: scale(1.1);
            box-shadow: 0 10px 20px rgba(255, 0, 0, 0.3);
        }

        .btn-outline {
            border: 2px solid white;
            color: white;
            background: transparent;
        }

        .btn-outline:hover {
            background: white;
            color: var(--deep-aqua);
        }

        /* --- YouTube Feed --- */
        section { padding: 100px 10%; }

        .section-title {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 50px;
        }

        #video-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .video-card {
            background: var(--glass-bg);
            border-radius: 15px;
            overflow: hidden;
            transition: var(--transition);
            border: 1px solid rgba(255,255,255,0.1);
        }

        .video-card:hover { transform: translateY(-10px); }

        .video-card iframe {
            width: 100%;
            aspect-ratio: 16/9;
            border: none;
        }

        .video-info { padding: 20px; }

        /* --- About Section --- */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 40px;
        }

        .stat-card {
            background: rgba(255,255,255,0.1);
            padding: 30px;
            border-radius: 15px;
            text-align: center;
        }

        .stat-card i {
            font-size: 2.5rem;
            color: var(--accent-red);
            margin-bottom: 15px;
        }

        /* --- Contact Form --- */
        .contact-container {
            max-width: 600px;
            margin: 0 auto;
            background: var(--glass-bg);
            padding: 40px;
            border-radius: 20px;
            backdrop-filter: blur(5px);
        }

        .form-group { margin-bottom: 20px; }

        input, textarea {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid rgba(255,255,255,0.3);
            background: rgba(255,255,255,0.1);
            color: white;
            outline: none;
        }

        input::placeholder, textarea::placeholder { color: rgba(255,255,255,0.7); }

        /* --- Footer --- */
        footer {
            background: rgba(0,0,0,0.2);
            padding: 50px 10%;
            text-align: center;
        }

        .social-links {
            font-size: 1.5rem;
            margin-bottom: 20px;
        }

        .social-links a {
            color: white;
            margin: 0 15px;
            transition: var(--transition);
        }

        .social-links a:hover { color: var(--accent-red); }

        /* --- Mobile Responsiveness --- */
        @media (max-width: 768px) {
            .hamburger { display: block; }
            .nav-links {
                position: fixed;
                top: 80px;
                right: -100%;
                background: #00bcd4;
                width: 100%;
                height: calc(100vh - 80px);
                flex-direction: column;
                align-items: center;
                padding-top: 50px;
                transition: 0.5s;
            }
            .nav-links.active { right: 0; }
            .nav-links li { margin: 20px 0; }
            .cta-group { flex-direction: column; width: 100%; padding: 0 20px; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">JUNGLE<span>TIERS</span></div>
        <ul class="nav-links">
            <li><a href="#hero">Home</a></li>
            <li><a href="#videos">Latest Tiers</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
        <div class="hamburger">
            <i class="fas fa-bars"></i>
        </div>
    </nav>

    <section id="hero">
        <h1>UNLEASH THE TIER LIST</h1>
        <p style="font-size: 1.2rem; margin-bottom: 30px; max-width: 600px;">The ultimate destination for competitive rankings, community tiers, and deep-dive meta analysis.</p>
        <div class="cta-group">
            <a href="#videos" class="btn btn-primary">Watch Rankings</a>
            <a href="#contact" class="btn btn-outline">Join Discord</a>
        </div>
    </section>

    <section id="videos">
        <h2 class="section-title">Latest Updates</h2>
        <div id="video-container">
            <p style="text-align: center; grid-column: 1/-1;">Loading latest transmissions...</p>
        </div>
    </section>

    <section id="about">
        <h2 class="section-title">About Jungle Tiers</h2>
        <p style="text-align: center; max-width: 800px; margin: 0 auto;">Founded in 2024, Jungle Tiers provides high-octane competitive analysis for the community. We break down the meta so you don't have to.</p>
        <div class="stats-grid">
            <div class="stat-card">
                <i class="fas fa-users"></i>
                <h3>50K+</h3>
                <p>Subscribers</p>
            </div>
            <div class="stat-card">
                <i class="fas fa-eye"></i>
                <h3>2M+</h3>
                <p>Total Views</p>
            </div>
            <div class="stat-card">
                <i class="fas fa-trophy"></i>
                <h3>100+</h3>
                <p>Tier Lists</p>
            </div>
        </div>
    </section>

    <section id="contact">
        <h2 class="section-title">Get In Touch</h2>
        <div class="contact-container">
            <form id="contactForm">
                <div class="form-group">
                    <input type="text" placeholder="Your Name" required>
                </div>
                <div class="form-group">
                    <input type="email" placeholder="Your Email" required>
                </div>
                <div class="form-group">
                    <textarea rows="5" placeholder="Your Message" required></textarea>
                </div>
                <button type="submit" class="btn btn-primary" style="width: 100%;">Send Message</button>
            </form>
        </div>
    </section>

    <footer>
        <div class="social-links">
            <a href="#"><i class="fab fa-youtube"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="#"><i class="fab fa-discord"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
        </div>
        <p>&copy; 2026 Jungle Tiers. All Rights Reserved.</p>
    </footer>

    <script>
        // --- Mobile Menu Toggle ---
        const hamburger = document.querySelector('.hamburger');
        const navLinks = document.querySelector('.nav-links');

        hamburger.addEventListener('click', () => {
            navLinks.classList.toggle('active');
            hamburger.querySelector('i').classList.toggle('fa-bars');
            hamburger.querySelector('i').classList.toggle('fa-times');
        });

        // Close menu when clicking a link
        document.querySelectorAll('.nav-links a').forEach(link => {
            link.addEventListener('click', () => {
                navLinks.classList.remove('active');
                hamburger.querySelector('i').classList.add('fa-bars');
                hamburger.querySelector('i').classList.remove('fa-times');
            });
        });

        // --- Fetch YouTube Feed via RSS2JSON ---
        // Replace 'CHANNEL_ID' with your actual YouTube Channel ID (e.g., UC...)
        const CHANNEL_ID = 'UC_x5XG1OV2P6uYZ5FHSx6_A'; // Example: Google Developers ID
        const RSS_URL = `https://www.youtube.com/feeds/videos.xml?channel_id=${CHANNEL_ID}`;
        const API_URL = `https://api.rss2json.com/v1/api.json?rss_url=${encodeURIComponent(RSS_URL)}`;

        async function fetchVideos() {
            try {
                const response = await fetch(API_URL);
                const data = await response.json();
                const container = document.getElementById('video-container');
                container.innerHTML = ''; // Clear loading text

                if (data.items) {
                    // Get only first 3 videos
                    data.items.slice(0, 3).forEach(item => {
                        // YouTube RSS guid format is yt:video:VIDEO_ID
                        const videoId = item.guid.split(':').pop();
                        
                        const card = document.createElement('div');
                        card.className = 'video-card';
                        card.innerHTML = `
                            <iframe 
                                src="https://www.youtube.com/embed/${videoId}" 
                                allowfullscreen>
                            </iframe>
                            <div class="video-info">
                                <h4 style="margin-bottom:10px">${item.title}</h4>
                                <p style="font-size:0.8rem; opacity:0.8">${new Date(item.pubDate).toLocaleDateString()}</p>
                            </div>
                        `;
                        container.appendChild(card);
                    });
                }
            } catch (error) {
                console.error('Error fetching videos:', error);
                document.getElementById('video-container').innerHTML = '<p>Failed to load videos. Check back later!</p>';
            }
        }

        // --- Contact Form Submission ---
        document.getElementById('contactForm').addEventListener('submit', (e) => {
            e.preventDefault();
            alert('Thanks for reaching out! Our team will get back to you soon.');
            e.target.reset();
        });

        // Initialize
        fetchVideos();

        // Smooth Scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>
</html>